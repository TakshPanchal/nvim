#include<iostream>
#include<bits/stdc++.h>

#define ll long long
#define fori(i, l, r) for(ll i = l; i < r; i++)
#define ford(i, l, r) for(ll i = l; i > r; i--)

using namespace std;

int main(){
    int T = 0;
    cin >> T;
    while(T > 0){
        // code goes here
        T--;
    }
    return 0;
}
